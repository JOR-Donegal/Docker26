# Other Platforms

We could be creating images on a MAC, intended for publishing on a Linux VM.

Or we could be on a PC.

In __docker build__ there is an option for _platform_.

I normally avoid all of this, I develop on a Linux/amd64 VM and all my publishing is to Linux/amd64.
